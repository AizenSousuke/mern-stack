const initialState = {
    busStopCode: null,
    services: []
}

export const busStopReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}